{
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "artifact_id": "VER-QUIC-API-0005",
  "artifact_type": "verification",
  "status": "landed",
  "title": "Incursa.Quic Pending Outbound Stream Open Verification",
  "owner": "quic-maintainers",
  "domain": "quic",
  "verification_method": "Requirement-home execution, inspection, and trace render/check for the pending outbound open slice on the supported active loopback path.",
  "related_artifacts": [
    "SPEC-QUIC-API",
    "ARC-QUIC-API-0005",
    "WI-QUIC-API-0005"
  ],
  "verifies": [
    "REQ-QUIC-API-0014"
  ],
  "preconditions": [
    "The supported active loopback path and later real `MAX_STREAMS` growth path already exist.",
    "The runtime can keep a blocked outbound open request queued instead of failing fast.",
    "The existing cancellation and terminal-state semantics remain owned by `REQ-QUIC-API-0008`."
  ],
  "scope": "This artifact covers the pending outbound open slice only: a blocked outbound open remains incomplete when the peer limit is exhausted, completes after later real `MAX_STREAMS` growth makes the requested direction available, and still honors cancellation while pending. It does not claim `STREAMS_BLOCKED` emission, broader stream-management parity, `0-RTT`, key update, or retry changes.",
  "expected_result": "The requirement-home proof shows a blocked outbound open staying pending until later real `MAX_STREAMS` growth resumes it, the request cancellation path still settles a blocked pending open, and the trace/doc artifacts stay aligned with the new requirement ID and supporting architecture/work-item records.",
  "procedure": [
    "Run `tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0014.cs` to prove blocked pending open behavior, later real `MAX_STREAMS` wake-up, and blocked-open cancellation.",
    "Run the existing `REQ-QUIC-API-0008` cancellation coverage and `REQ-QUIC-API-0009` stream-capacity coverage to keep the adjacent open-stream and capacity slices green.",
    "Run `git diff --check`.",
    "Run the repo-local SpecTrace render/check flow for the changed canonical JSON artifacts and confirm the generated markdown views are current."
  ],
  "evidence": [
    "specs/requirements/quic/SPEC-QUIC-API.json",
    "specs/requirements/quic/REQUIREMENT-GAPS.md",
    "specs/architecture/quic/ARC-QUIC-API-0005.json",
    "specs/work-items/quic/WI-QUIC-API-0005.json",
    "src/Incursa.Quic/QuicConnectionRuntime.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeEventModels.cs",
    "src/Incursa.Quic/QuicConnectionStreamState.cs",
    "src/Incursa.Quic/QuicFrameCodec.cs",
    "tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0008.cs",
    "tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0009.cs",
    "tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0014.cs"
  ],
  "status_summary": "Landed; this verification slice records the proof burden for keeping blocked outbound opens pending until later real `MAX_STREAMS` growth resumes them without widening the public stream contract."
}
