---
artifact_id: "WI-QUIC-RFC9001-0006"
artifact_type: "work_item"
title: "QUIC RFC 9001 Repeated Epoch Ownership Follow-On Work Item"
domain: "quic"
status: "planned"
owner: "quic-maintainers"
addresses:
  - "REQ-QUIC-RFC9001-S6P5-0006"
design_links:
  - "ARC-QUIC-RFC9001-0006"
verification_links:
  - "VER-QUIC-RFC9001-0006"
related_artifacts:
  - "SPEC-QUIC-RFC9001"
  - "ARC-QUIC-RFC9001-0006"
  - "VER-QUIC-RFC9001-0006"
  - "ARC-QUIC-RFC9001-0003"
  - "WI-QUIC-RFC9001-0003"
  - "VER-QUIC-RFC9001-0003"
---

<!-- Generated from sibling .json by scripts/spec-trace/Render-SpecTraceMarkdownFromJson.ps1. Do not edit this file directly. -->

# WI-QUIC-RFC9001-0006 - QUIC RFC 9001 Repeated Epoch Ownership Follow-On Work Item

## Summary

Trace and prove the unbounded repeated epoch ownership model that keeps current, next, and retained-old 1-RTT state distinct while repeated updates remain active.

## Requirements Addressed

- REQ-QUIC-RFC9001-S6P5-0006

## Design Inputs

- ARC-QUIC-RFC9001-0006

## Planned Changes

- Make the repeated epoch ownership model explicit instead of relying on representative later-epoch cleanup alone.
- Keep current, next, and retained-old 1-RTT ownership separate in the `QuicOneRttKeyUpdateLifecycle` seam.
- Preserve current-phase acknowledgment and repeated-update cooldown ownership when retained-old material is discarded.
- Keep sender/recovery cleanup synchronized with retained-old discard.
- Preserve the representative later-epoch cleanup benchmark and fuzz/property coverage as bounded evidence only.
- Keep broader stateless-reset response behavior outside this follow-on.

## Out of Scope

- Broader stateless-reset response behavior outside the retained-route floor
- New stateless-reset formatting behavior
- Token generation or token storage changes
- Any non-QUIC transport changes

## Verification Plan

Run the representative later-epoch requirement-home tests and the repeated old-key cleanup benchmark cases, add any missing negative or fuzz coverage if the ownership model exposes a new seam, render the touched SpecTrace Markdown views from JSON, and finish with git diff --check.

## Completion Notes

Planned. The current runtime only proves representative later-epoch cleanup across phases 6 through 32; this follow-on exists to make the unbounded repeated epoch ownership model explicit and keep sender/recovery cleanup traceable.

## Trace Links

Addresses:

- REQ-QUIC-RFC9001-S6P5-0006

Uses Design:

- ARC-QUIC-RFC9001-0006

Verified By:

- VER-QUIC-RFC9001-0006
