---
artifact_id: "WI-QUIC-RFC9001-0004"
artifact_type: "work_item"
title: "QUIC RFC 9001 Post-Stop Stateless Reset Response Work Item"
domain: "quic"
status: "planned"
owner: "quic-maintainers"
addresses:
  - "REQ-QUIC-RFC9001-S6P6-0006"
design_links:
  - "ARC-QUIC-RFC9001-0004"
verification_links:
  - "VER-QUIC-RFC9001-0004"
related_artifacts:
  - "SPEC-QUIC-RFC9001"
  - "ARC-QUIC-RFC9001-0004"
  - "VER-QUIC-RFC9001-0004"
  - "ARC-QUIC-CRT-0040"
  - "WI-QUIC-CRT-0042"
  - "VER-QUIC-CRT-0042"
---

<!-- Generated from sibling .json by scripts/spec-trace/Render-SpecTraceMarkdownFromJson.ps1. Do not edit this file directly. -->

# WI-QUIC-RFC9001-0004 - QUIC RFC 9001 Post-Stop Stateless Reset Response Work Item

## Summary

Implement and prove the retained-route stateless-reset response floor after AEAD-limit terminal discard, keeping the same-remote-address-and-port lookup, loop-prevention, and rate-budget gates aligned across the endpoint helper and host shells.

## Requirements Addressed

- REQ-QUIC-RFC9001-S6P6-0006

## Design Inputs

- ARC-QUIC-RFC9001-0004

## Planned Changes

- Keep the retained-route response floor exact after AEAD-limit terminal discard.
- Align endpoint-helper emission, connected endpoint host forwarding, and listener host forwarding on the same retained-route lookup, token ownership, loop-prevention, and rate-budget gates.
- Add requirement-home coverage for matching-route emission, remote-port mismatch suppression, loop-prevention suppression, and rate-budget suppression.
- Refresh the permanent `QuicStatelessResetBenchmarks` suite as the hot-path evidence source for retained-route lookup hit/miss and stateless-reset formatting.
- Keep broader stateless-reset response behavior outside the retained-route floor blocked until separately traced.

## Out of Scope

- Broader stateless-reset response behavior outside the retained-route floor
- New token-generation behavior
- New stateless-reset formatting behavior
- A separate ambiguity-detection engine
- Key-update lifecycle broadening

## Verification Plan

Run focused requirement-home tests for the new retained-route floor, include negative suppression coverage for remote-port mismatch, loop-prevention, and rate budget failures, refresh the stateless-reset benchmark suite, render the touched SpecTrace markdown views from JSON, and finish with git diff --check.

## Completion Notes

Planned for the retained-route response floor only. The broader response matrix remains open for any behavior that does not resolve through the retained route, remembered token, and loop/rate gates.

## Trace Links

Addresses:

- REQ-QUIC-RFC9001-S6P6-0006

Uses Design:

- ARC-QUIC-RFC9001-0004

Verified By:

- VER-QUIC-RFC9001-0004
