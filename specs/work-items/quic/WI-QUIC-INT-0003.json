{
  "title": "QUIC Interop Transfer Completion Work Item",
  "verification_links": [
    "VER-QUIC-INT-0003"
  ],
  "owner": "quic-maintainers",
  "related_artifacts": [
    "SPEC-QUIC-INT",
    "SPEC-QUIC-CRT",
    "ARC-QUIC-INT-0003",
    "VER-QUIC-INT-0003"
  ],
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "completion_notes": "Implemented and revalidated locally on 2026-04-22. The transfer slice still routes the ordered client `REQUESTS` list through one managed connection, and the server-role path now also tolerates the runner's empty server-side `REQUESTS` list by deriving mounted source paths from the actual HTTP/0.9 request targets it receives. Requirement-home proof now covers that empty-server-REQUESTS dispatch shape, the preserved server-role helper bundle at `artifacts/interop-runner/20260422-140138237-server-nginx/` narrowed the regression before the fix by showing the harness failed fast on `REQUESTS must contain at least one URL for testcase dispatch.`, and the post-fix rerun at `artifacts/interop-runner/20260422-141632722-server-nginx/` shows the managed server completing every transfer response with both endpoints exiting `0` before the external runner's own post-check throws `FileNotFoundError`.",
  "out_of_scope": [
    "Retry enablement in the QUIC interop runner.",
    "Parallel or arbitrary multi-stream behavior.",
    "General-purpose file-server behavior.",
    "Broader 1-RTT data-path claims, trust-store policy widening, hostname validation, certificate-path validation, revocation, 0-RTT, or key update."
  ],
  "status": "complete",
  "domain": "quic",
  "summary": "Implement the ordered one-connection child-process transfer contract on the existing active-phase managed path without widening the public or unsupported boundary.",
  "verification_plan": "Run the focused `REQ_QUIC_INT_0010`, `REQ_QUIC_INT_0014`, replay, and helper-script lanes, rerun the fresh client-role quic-go transfer helper plus the server-role quic-go transfer helper, and confirm the harness only reports exit `0` after the full ordered request list reaches byte delivery plus EOF on both sides while supported server-role runs tolerate the runner's empty server-side `REQUESTS` list. Keep retry, multiconnect, msquic, and broader transfer widening on their own slices.",
  "supplemental_sections": [
    {
      "heading": "Related Code And Tests",
      "content": "- [`InteropHarnessRunner.cs`](../../../src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs)\n- [`InteropHarnessEnvironment.cs`](../../../src/Incursa.Quic.InteropHarness/InteropHarnessEnvironment.cs)\n- [`InteropEndpointHost.cs`](../../../src/Incursa.Quic.InteropHarness/InteropEndpointHost.cs)\n- [`QuicConnectionRuntime.cs`](../../../src/Incursa.Quic/QuicConnectionRuntime.cs)\n- [`QuicStream.cs`](../../../src/Incursa.Quic/QuicStream.cs)\n- [`QuicClientConnectionHost.cs`](../../../src/Incursa.Quic/QuicClientConnectionHost.cs)\n- [`QuicListenerHost.cs`](../../../src/Incursa.Quic/QuicListenerHost.cs)\n- [`QuicTlsKeySchedule.cs`](../../../src/Incursa.Quic/QuicTlsKeySchedule.cs)\n- [`REQ-QUIC-CRT-0121.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0121.cs)\n- [`REQ-QUIC-API-0010.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0010.cs)\n- [`REQ-QUIC-INT-0008.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0008.cs)\n- [`REQ-QUIC-INT-0010.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0010.cs)"
    }
  ],
  "artifact_type": "work_item",
  "planned_changes": "- Route the `transfer` testcase through the existing active-phase managed path as an ordered client-side `REQUESTS` plan on one managed connection.\n- Keep the server-side transfer path honest when the external runner passes `REQUESTS_SERVER=\"\"` by deriving mounted source paths from the actual HTTP/0.9 request targets instead of failing before QUIC work starts.\n- Open or accept exactly one application stream per ordered request and gate exit `0` on byte delivery plus EOF across the full ordered request list.\n- Preserve the fixed `/www` and `/downloads` mount-path contract without widening retry, parallel stream fan-out, or TLS policy.\n- Add requirement-home coverage, captured-interop replay coverage, and narrow helper validation for the ordered transfer contract while keeping the unsupported-testcase guards honest.",
  "design_links": [
    "ARC-QUIC-INT-0003"
  ],
  "addresses": [
    "REQ-QUIC-INT-0010"
  ],
  "artifact_id": "WI-QUIC-INT-0003"
}
