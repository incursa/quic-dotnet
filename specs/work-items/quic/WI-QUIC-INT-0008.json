{
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "artifact_id": "WI-QUIC-INT-0008",
  "artifact_type": "work_item",
  "status": "complete",
  "title": "QUIC Interop Runner Multiconnect Sequential Dispatch Work Item",
  "owner": "quic-maintainers",
  "domain": "quic",
  "summary": "Route the `multiconnect` testcase through the existing managed endpoint-host transfer path sequentially, add server-role open-plan coverage, apply a bounded child-process loss timing budget, and keep unsupported, stalled-response, and replay-narrowed teardown cases honest.",
  "related_artifacts": [
    "SPEC-QUIC-INT",
    "SPEC-QUIC-CRT",
    "ARC-QUIC-INT-0008",
    "VER-QUIC-INT-0008"
  ],
  "design_links": [
    "ARC-QUIC-INT-0008"
  ],
  "verification_links": [
    "VER-QUIC-INT-0008"
  ],
  "addresses": [
    "REQ-QUIC-INT-0015"
  ],
  "planned_changes": "- Add a sequential `multiconnect` plan builder to `InteropHarnessRunner` that validates each client `REQUESTS` URL, requires a shared `https` host/port, and opens one managed connection per request.\n- Keep the client and server paths on the existing endpoint-host transfer path rather than introducing a generalized multipath dispatcher.\n- Bound the multiconnect client handshake and advertised idle budget for the runner's heavy-loss `handshakeloss` cell without changing the public 10-second handshake default.\n- Bound the client-side response wait so a stalled sequential response fails the child process honestly instead of hanging until the external runner times out.\n- Allow the server-role external-runner shape to use an empty `REQUESTS` list as an open sequential accept plan, derive the mounted path from each inbound HTTP/0.9 request target, and send the response after the request line instead of waiting for request EOF.\n- Replace server-initiated application close after a completed response with bounded peer-close linger, and run open-plan linger off the accept path so a completed connection cannot block later sequential accepts.\n- Keep the listener alive across per-peer UDP reset/refused indications on the shared server socket.\n- Add requirement-home coverage that proves the sequential happy path, qlog capture, the server open-plan accept path, the child-process loss timing budget, the stalled-response failure path, and malformed-URL rejection.\n- Update local interop helper docs and testcase whitelists so `multiconnect` is recognized honestly.",
  "out_of_scope": [
    "parallel connection handling",
    "migration or rebinding support",
    "retry enablement",
    "`0-RTT`",
    "key update",
    "public API widening",
    "external runner registry changes",
    "benchmark work",
    "fuzzing work"
  ],
  "verification_plan": "Run the `REQ_QUIC_INT_0015` lane, confirm the sequential connection markers and qlog capture on the smoke path, confirm malformed multiconnect input fails before transport success is claimed, confirm a stalled response fails with exit code `1` and no preserved partial download, confirm the multiconnect client applies the bounded child-process loss timing budget without changing the public default, and confirm the empty-server-REQUESTS open-plan path can accept a later sequential connection while a completed prior connection lingers. Re-run once if the tree is temporarily mid-edit and a first pass fails for an unrelated build reason.",
  "completion_notes": "Implemented and verified; the requirement-home `REQ-QUIC-INT-0015` lane passed locally on 2026-04-21 with sequential smoke, malformed-input, and stalled-response honest-failure coverage, then was widened on 2026-04-22 with preserved server-role replay coverage for empty-server-REQUESTS open-plan sequencing, response-after-request-line handling, bounded post-response linger, no server-initiated application close after response, and shared-listener socket reset tolerance. Hosted supported-subset run 25186263470 on 2026-04-30 proved the retry, transfer, and handshake cells but failed the client-role multiconnect cell on connection 21/50 because the client still inherited the public 10-second handshake default under handshakeloss while Initial and Handshake packets were still being exchanged; this follow-up applies a bounded child-process timing budget to that lane only."
}
