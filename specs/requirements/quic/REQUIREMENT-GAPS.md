---
workbench:
  type: specification
  workItems: []
  codeRefs: []
  pathHistory: []
  path: /specs/requirements/quic/REQUIREMENT-GAPS.md
---

# QUIC Requirement Gaps

This ledger tracks open questions, ambiguities, and follow-up decisions for QUIC requirement work.

## Open Gaps

- `interop-all-upstream-client-nonhandshake-xquic-liveness` is open as the remaining local client-role all-upstream `transfer`/`keyupdate`/`chacha20` residual after the May 19, 2026 refresh and is now trace-owned by `REQ-QUIC-INT-0029`, `ARC-QUIC-INT-0023`, `WI-QUIC-INT-0023`, and `VER-QUIC-INT-0023`. The generic stream-data liveness bug is closed by the focused ordered-read wake-up proof and by live quic-go `transfer`, `chacha20`, and `keyupdate` reruns. The ACK scheduler now has focused proof that the two-packet delayed-ACK threshold re-arms after a prior ACK while still delaying after only one new ack-eliciting packet. The broad client-role run under `.artifacts/interop-runner/all-upstream-streamdata-keyupdate-local/client/20260519-121832639-client-chrome` now passes `transfer` and `keyupdate` for 15 of 16 upstream server-capable peers, passes `chacha20` for 14 of 16, and reports `chacha20` unknown/unsupported for `mvfst` and `go-x-net`. The only failed peer cluster is `xquic`, where all three cells time out before the first generated response body completes. That preserved May 19 run shows `transfer`, `keyupdate`, and `chacha20` all timing out after 35,400 bytes at the response-stream FIN boundary. A focused `xquic` rerun after the ACK-threshold topoff timed out under `.artifacts/interop-runner/debug-client-transfer-xquic-after-ack-threshold/20260519-140609871-client-chrome` after 35,400 bytes; a fresh May 22, 2026 rerun under `.artifacts/interop-runner/debug-client-transfer-xquic-current-after-docker-cleanup/20260522-221632093-client-chrome` timed out after 14,174 bytes of a 2,097,152-byte first generated response body, while the simulator captured client/server packet paths with zero interface drops. A fresh May 23, 2026 rerun under `.artifacts/interop-runner/debug-client-transfer-xquic-live-refresh/20260523-161053951-client-chrome` failed after the client read 15,355 bytes, and the xquic server log still only decrypts/processes client short-header packets `0..3`. The May 23 captured-packet proofs open client-to-xquic 1-RTT packets from that rerun, verify ACK coverage through xquic response packet 17 plus a MAX_DATA frame, and verify an alternating repeated MAX_DATA/MAX_STREAM_DATA sequence through client packet 28 with increasing limits. A bounded artifact comparison found both simulator pcaps contain decryptable client short-header packets `0..40`, while xquic's own server log only decrypts/processes client short-header packets `0..3`, so the next slice should prioritize post-send drain, pacing wakeup, peer delivery, xquic event-loop packet consumption, and stream-drain behavior before changing ACK generation, packet protection, or local credit publication. The fresh live rerun also reaches `xqc_process_read_streams|stream_read_notify|flag:66` after the request stream is fully received, and the server-side response burst runs through packet 22 before `xqc_send_packet_pacer_allows|pacing blocked` at `stream_offset:16536` prevents the transfer from draining to FIN. The local runner helper now reports that preserved packet/key material confirms valid post-burst ACK traffic while xquic's server-log short-header packet-processing ceiling remains a non-promoting failure diagnostic while keeping the run failed. Treat this as a reduced xquic-specific liveness investigation; it is not HTTP/3, migration, server-role non-handshake, or support-readiness proof.

- `interop-all-upstream-client-nonhandshake-xquic-liveness-fresh-rerun` captures the fresh May 23, 2026 `keyupdate`/`chacha20` rerun under `.artifacts/interop-runner/debug-client-keyupdate-chacha20-xquic-live-refresh/20260523-192303830-client-chrome`, which also failed after the client read 15,355 bytes. That keeps the newest runtime evidence aligned across transfer, keyupdate, and chacha20 and confirms the residual still belongs to the shared xquic non-handshake ceiling rather than a transfer-only artifact.
- The local runner helper now also has a shared-ceiling fallback for `keyupdate` and `chacha20` when the server log does not expose the deeper packet markers, so the non-promoting diagnostic stays useful even when the broad server-side packet breadcrumbs are absent.
- `non-http3-interop-suite-inventory` remains open for cells that have not been promoted by their own proof slice. `rebind-port` and `rebind-addr` are RFC 9000 migration/path-validation interop cells, not a different RFC. `rebind-port` is promoted by the June 11, 2026 direct runner artifact at `artifacts/interop-runner/20260611-085254961-direct-rebind-port-cid-warmup-tools-patched`, which records `quic-go`/`chrome` `rebind-port` as `succeeded`, a 10 MiB managed transfer completion, two observed server paths, and qlog-backed PATH_CHALLENGE/PATH_RESPONSE evidence for the rebound port. `rebind-addr` is promoted by the June 11, 2026 direct runner artifact at `artifacts/interop-runner/20260611-090616967-direct-rebind-addr-cid-warmup-tools-patched`, which records `quic-go`/`chrome` `rebind-addr` as `succeeded`, a 10 MiB managed transfer completion, three observed server paths, and qlog-backed PATH_CHALLENGE/PATH_RESPONSE evidence for the rebound addresses. `transfercorruption` is promoted by the June 11, 2026 helper-backed runner artifact at `artifacts/interop-runner/20260611-094544073-client-chrome`, which records quic-go/chrome `transfercorruption` as `succeeded` under moderate packet corruption. `handshakecorruption` remains an explicit prerequisite-blocked inventory cell rather than a generic missing entry; the June 11, 2026 helper-backed diagnostic at `artifacts/interop-runner/20260611-092726774-client-chrome` mapped through the upstream multiconnect-derived corruption flow and failed with local client `FRAME_ENCODING_ERROR`/unknown frame type `0x23de` evidence. ECN remains outside the helper-supported inventory until its own proof slice promotes it.

## Future RFC Planning Gaps

- `http3-adapter-boundary` is narrowed by `SPEC-QUIC-RFC9114` and the bounded QUIC interop-runner `TESTCASE=http3` endpoint adapter and optional HTTP/3 qlog diagnostics, by `REQ-QUIC-INT-0031`, `ARC-QUIC-INT-0025`, `WI-QUIC-INT-0025`, and `VER-QUIC-INT-0025` for the advisory external HTTP/3 peer harness/reporting surface with retained qlogs, TLS key logs, stdout/stderr, decoded summaries, and caller-supplied pcaps, and by `REQ-QUIC-INT-0032`, `ARC-QUIC-INT-0026`, `WI-QUIC-INT-0026`, and `VER-QUIC-INT-0026` for advisory h3spec server conformance triage. The HTTP/3 frame, stream, SETTINGS, header-validation, malformed-sequence, and minimal request/response floor is now trace-owned by `SPEC-QUIC-RFC9114`, `ARC-QUIC-RFC9114-0001`, `ARC-QUIC-RFC9114-0002`, `WI-QUIC-RFC9114-0001`, `WI-QUIC-RFC9114-0002`, `VER-QUIC-RFC9114-0001`, and `VER-QUIC-RFC9114-0002`. Pressure class: `HTTP/3 adapter`, `stream adapter`, and `diagnostics/manageability`. Allocation-sensitive seams: effect emission and stream delivery across the transport boundary, especially `QuicConnectionRuntimeEventModels` and `QuicListenerHost`.

- `qpack-stream-state-boundary` is narrowed by `SPEC-QUIC-RFC9204` and the bounded `TESTCASE=http3` runner adapter, which uses the standalone QPACK module through the HTTP/3 layer without dynamic table emission in the runner path, and by the h3spec triage parser that maps QPACK failures back to RFC 9204 sections without promoting them as fixed. The bounded runner adapter is trace-owned by `REQ-QUIC-INT-0030`, `ARC-QUIC-INT-0024`, `WI-QUIC-INT-0024`, and `VER-QUIC-INT-0024`. The standalone QPACK codec, static table, field-section encoding and decoding, dynamic table accounting, blocked-stream synchronization, and instruction-stream parsing are now trace-owned by `SPEC-QUIC-RFC9204`, `ARC-QUIC-RFC9204-0001`, `WI-QUIC-RFC9204-0001`, and `VER-QUIC-RFC9204-0001`. Pressure class: `stream adapter` and `HTTP/3 adapter`. Allocation-sensitive seams: stream-state capture and restore, especially `QuicConnectionStreamSnapshot` and `QuicConnectionStreamState`.

- `websockets-http3-extended-connect` is narrowed by `SPEC-QUIC-RFC9220`, `ARC-QUIC-RFC9220-0001`, `WI-QUIC-RFC9220-0001`, and `VER-QUIC-RFC9220-0001` for SETTINGS_ENABLE_CONNECT_PROTOCOL value/default handling, RFC 8441-style Extended CONNECT request pseudo-header validation, unsupported-protocol 501 responses with optional problem-details payloads, and FIN versus H3_REQUEST_CANCELLED closure mapping. Remaining work covers full WebSocket frame parsing, masking, message reassembly, bidirectional tunnel forwarding, and application-level WebSocket handler integration. Pressure class: `HTTP/3 adapter` and `stream adapter`. Current action is implementation only for explicitly trace-owned slices; otherwise keep remaining WebSocket tunnel work in planning until support is committed. Allocation-sensitive seams: HTTP/3 stream delivery and adapter effect emission above the core transport runtime.

- `http-datagrams-and-capsules` is closed by `SPEC-QUIC-RFC9297`, `ARC-QUIC-RFC9297-0001` through `ARC-QUIC-RFC9297-0003`, `WI-QUIC-RFC9297-0001` through `WI-QUIC-RFC9297-0003`, and `VER-QUIC-RFC9297-0001` through `VER-QUIC-RFC9297-0003`. The RFC 9297 requirement set is now trace-owned across HTTP Datagram byte layout, SETTINGS_H3_DATAGRAM and H3_DATAGRAM_ERROR registration, base Capsule and DATAGRAM Capsule encoding, Capsule Protocol response/header policy, Capsule Type registry invariants, HTTP Datagram request lifecycle, HTTP/1.x start policy, flow-control and buffering guidance, Capsule payload length validation, DATAGRAM Capsule semantics, intermediary conversion decisions, extension limit handling, and new-extension Capsule Protocol preference. The remaining repository work in this file concerns other RFCs and future adapter integration, not RFC 9297 compliance.

- `connect-udp` is narrowed by `SPEC-QUIC-RFC9298`, `ARC-QUIC-RFC9298-0001`, `WI-QUIC-RFC9298-0001`, and `VER-QUIC-RFC9298-0001` for RFC 9298 URI Template level/form validation, printable-ASCII enforcement, required `target_host` and `target_port` variables, default template construction, target host and port validation, IPv6 scoped-zone rejection, IPv6 literal percent-encoding, and request path/query expansion. It is further narrowed by `ARC-QUIC-RFC9298-0002`, `WI-QUIC-RFC9298-0002`, and `VER-QUIC-RFC9298-0002` for HTTP/3 CONNECT-UDP request construction, the `connect-udp` Extended CONNECT token, Capsule-Protocol enforcement, no-content request/successful-response policy, 2xx successful-response policy, and client proceed/abort decision, by `ARC-QUIC-RFC9298-0003`, `WI-QUIC-RFC9298-0003`, and `VER-QUIC-RFC9298-0003` for UDP Proxying HTTP Datagram Context ID layout, 62-bit varint bounds, Context ID allocation parity and request-namespace scoping, unknown Context ID handling, Context ID zero UDP payload semantics, oversized payload rejection, known-MTU discard policy, and DATAGRAM Capsule discard policy, by `ARC-QUIC-RFC9298-0004`, `WI-QUIC-RFC9298-0004`, and `VER-QUIC-RFC9298-0004` for vulnerable-target filtering and `destination_ip_prohibited` Proxy-Status reporting, and by `ARC-QUIC-RFC9298-0005`, `WI-QUIC-RFC9298-0005`, and `VER-QUIC-RFC9298-0005` for burstiness, batching, congestion-control, QUIC DATAGRAM, oversized-payload, ICMP Packet Too Big recommendation, inner IP header, and ECN forwarding policy. Remaining work covers non-multiplexed stream interpretation, HTTP/1.1 upgrade behavior, tunnel setup, early-datagram handling, and live UDP socket lifecycle/forwarding. Pressure class: `datagram adapter`, `HTTP/3 adapter`, and `stream adapter`. Allocation-sensitive seams: UDP payload ownership and HTTP Datagrams bridging, especially the same outbound datagram path used by `QuicConnectionRuntime` and `QuicListenerHost`.

- `connect-ip-masque` is a planning gap for `rfc9484.txt` sections `4.1`, `4.7`, `6`, `7`, `10`, `12.1`-`12.4`. Pressure class: `datagram adapter`, `HTTP/3 adapter`, and `diagnostics/manageability`. Current action is architecture planning only unless support is explicitly committed later. Allocation-sensitive seams: outbound datagram ownership, packet handling, and MTU-sensitive proxy flow, especially `QuicConnectionRuntimeEventModels.QuicConnectionSendDatagramEffect` and the adapter boundary above `QuicConnectionRuntime`.

- `dns-over-quic` is closed by `SPEC-QUIC-RFC9250`, `ARC-QUIC-RFC9250-0001` through `ARC-QUIC-RFC9250-0022`, `WI-QUIC-RFC9250-0001` through `WI-QUIC-RFC9250-0022`, and `VER-QUIC-RFC9250-0001` through `VER-QUIC-RFC9250-0022`. The RFC 9250 requirement surface is now fully trace-owned and verified; the remaining repository work in this file concerns other RFCs and future planning gaps, not DoQ compliance.

- `encrypted-dns-discovery-and-provisioning` is narrowed by `SPEC-QUIC-RFC9461`, `ARC-QUIC-RFC9461-0001`, `WI-QUIC-RFC9461-0001`, and `VER-QUIC-RFC9461-0001` for RFC 9461 DNS SVCB service-name formation, authentication-name validation, per-transport default ports, and Port Prefix Naming, and by `ARC-QUIC-RFC9461-0002`, `WI-QUIC-RFC9461-0002`, and `VER-QUIC-RFC9461-0002` for RFC 9461 scalar endpoint selection across `alpn`, `dohpath`, `port`, HTTPS SvcParam carry-through, per-protocol default ports, DoH origin selection, and DoH path expansion, and by `ARC-QUIC-RFC9461-0003`, `WI-QUIC-RFC9461-0003`, and `VER-QUIC-RFC9461-0003` for dohpath HTTPS RR query suppression, operator HTTPS/AliasMode/TargetName guidance, secure transport authentication-name policy, DNS-query client identity suppression, invalid-response query suppression and logging policy, SVCB-reliant behavior, dependent-specification fallback adjustment, cleartext fallback discouragement, and dohpath/SVCB/port registry metadata. It is narrowed by `SPEC-QUIC-RFC9463`, `ARC-QUIC-RFC9463-0001`, `WI-QUIC-RFC9463-0001`, and `VER-QUIC-RFC9463-0001` for RFC 9463 encrypted DNS option code points, scalar lifetime semantics, required ADN validation, minimum usable resolver address validation, loopback/multicast address filtering, forbidden `ipv4hint`/`ipv6hint` SvcParam rejection, and 16-bit service priority storage, by `ARC-QUIC-RFC9463-0002`, `WI-QUIC-RFC9463-0002`, and `VER-QUIC-RFC9463-0002` for DHCPv6 OPTION_V6_DNR TLV encoding and parsing, RFC 8415 DNS-name wire formatting, IPv6 address length validation, RFC 9460 SvcParam carry-through, ADN-only mode, OPTION_ORO request data, multiple resolver instances, service-priority sorting, and invalid-option discard behavior, and by `ARC-QUIC-RFC9463-0003`, `WI-QUIC-RFC9463-0003`, and `VER-QUIC-RFC9463-0003` for DHCPv4 OPTION_V4_DNR tag/length and repeated instance-data encoding, RFC 8415 DNS-name wire formatting, IPv4 address length validation, RFC 9460 SvcParam carry-through, ADN-only mode, RFC 3396 segmentation, Parameter Request List data, multiple resolver instances, service-priority sorting, and invalid-instance discard behavior, and by `ARC-QUIC-RFC9463-0004`, `WI-QUIC-RFC9463-0004`, and `VER-QUIC-RFC9463-0004` for Neighbor Discovery encrypted DNS option type/length encoding, lifetime semantics, RFC 8415 DNS-name wire formatting, IPv6 address length validation, RFC 9460 SvcParam carry-through, ADN-only mode, zero padding, multiple option processing, service-priority sorting, and invalid-option discard behavior, and by `ARC-QUIC-RFC9463-0005`, `WI-QUIC-RFC9463-0005`, and `VER-QUIC-RFC9463-0005` for RFC 8106 DHCP/RA source precedence, encrypted DNS preference over Do53, Do53 fallback after encrypted connection failure, discovered resolver priority ordering, PKIX plus ADN certificate-validation policy, and default versus explicit trust-anchor mode, and by `ARC-QUIC-RFC9463-0006`, `WI-QUIC-RFC9463-0006`, and `VER-QUIC-RFC9463-0006` for generic DNR information including ADN presence, resolver-address preference order, RFC 9460 SvcParam encoding, ADN-only mode, service priority, OPTION_V6_DNR ORO requestability and non-singleton metadata, and OPTION_V4_DNR option-data length. It is narrowed by `SPEC-QUIC-RFC9464`, `ARC-QUIC-RFC9464-0001`, `WI-QUIC-RFC9464-0001`, and `VER-QUIC-RFC9464-0001` for RFC 9464 scalar ENCDNS_IP* and ENCDNS_DIGEST_INFO attribute reserved-bit, length, zero-length request/ACK, address-family, ADN, forbidden hint SvcParam, shared service-parameter, attribute type, hash-algorithm count, and no-padding rules, by `ARC-QUIC-RFC9464-0002`, `WI-QUIC-RFC9464-0002`, and `VER-QUIC-RFC9464-0002` for RFC 9464 ENCDNS_IP* and ENCDNS_DIGEST_INFO byte-level codecs, RFC 9460 SvcParam wire key/length/value handling, digest layout validation, and SHA2-256 SPKI digest computation, and by `ARC-QUIC-RFC9464-0003`, `WI-QUIC-RFC9464-0003`, and `VER-QUIC-RFC9464-0003` for RFC 9464 support advertisements, request-specific resolver suggestions, optional digest requests, duplicate request handling, repeated-attribute discard policy, CFG_REPLY configuration selection, responder-controlled values, service-priority ordering, and optional digest-info replies, and by `ARC-QUIC-RFC9464-0004`, `WI-QUIC-RFC9464-0004`, and `VER-QUIC-RFC9464-0004` for client-side encrypted-resolver preference, conveyed-address session targeting, split-tunnel internal-domain routing, ADN and SHA2-256 SPKI digest certificate validation decisions, non-recoverable validation failure, malformed-attribute rejection, and NULL-authentication policy. Remaining planning work covers DNS SVCB wire RR parsing, operator DNS publication automation, full IKEv2 Configuration payload exchange integration, host resolver installation, and platform router/forwarder advertisement adapters. Pressure class: `DNS/discovery edge`. Current action is implementation only for explicitly trace-owned slices; otherwise keep remaining discovery/provisioning work in planning until support is committed. Allocation-sensitive seams: no QUIC hot-path seam; keep resolver/configuration data as scalar snapshots that enter the core only at bootstrap or endpoint selection time.

- `manageability-guidance-rfc9312` is now narrowed by `SPEC-QUIC-RFC9312`, `ARC-QUIC-RFC9312-0001`, `WI-QUIC-RFC9312-0001`, and `VER-QUIC-RFC9312-0001` for metadata-only diagnostics and qlog mapping across packet header observation, coalesced datagrams, connection ID lifecycle, migration/path validation, spin-bit state, ICMP Packet Too Big, PMTU updates, close/draining state, UDP errors, and anti-amplification blocking. Remaining RFC 9312 guidance that requires deployment policy, packet capture infrastructure, loss-bit support, DSCP/QoS, QUIC-LB, ECMP, or managed-network controls stays classified as documentation, not applicable, or future extension rather than runtime feature work. Pressure class: `diagnostics/manageability` and `interop/test-only`. Allocation-sensitive seams: diagnostics emission and packet/version observation, especially `QuicConnectionRuntime.DiagnosticsSink`, runtime event emission, and packet inspection helpers.

- `queued-stream-send-ownership` is closed by `REQ-QUIC-API-0010`, `VER-QUIC-API-0001`, and the RFC 9000 stream retransmission homes. The proof shows queued payload bytes stay owned and stable through delayed flush, promotion to final writes, loss handling, retransmission planning, and ack suppression before copy-elision work; `QuicSendPolicy` owns queued application-send budget decisions, `QuicApplicationSendScheduler` owns pure queued-work selection, and `QuicConnectionRuntime` remains the executor for packet protection, accounting, effects, and queue mutation. The verified slice preserves the measured four-datagram post-recovery burst cap, uses available congestion and anti-amplification bytes to bound recovery-progress queued sends, refreshes the policy snapshot after each protected queued send, and keeps local BenchmarkDotNet and ProtocolLab evidence explicitly non-publishable. It does not claim full byte/packet pacing.

## How To Use

- Add a gap here before implementation whenever RFC text leaves more than one plausible interpretation.
- Keep the note short and actionable.
- Reference the owning `SPEC-...` file and the follow-up artifact if one exists.
- For RFC9000 reference reconciliation, check the derived crosswalk and retired-ID ledger in `specs/generated/quic` before renaming or retiring any ID.
- Remove the gap entry once the owning requirement, architecture, work item, and verification artifacts carry the closed evidence.
