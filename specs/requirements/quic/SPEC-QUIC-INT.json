{
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "artifact_id": "SPEC-QUIC-INT",
  "artifact_type": "specification",
  "title": "QUIC Interop Harness Adapter and Endpoint Host Contract",
  "domain": "quic",
  "capability": "interop-harness",
  "status": "draft",
  "owner": "quic-maintainers",
  "related_artifacts": [
    "SPEC-QUIC-CRT",
    "ARC-QUIC-INT-0001",
    "ARC-QUIC-INT-0002",
    "WI-QUIC-INT-0001",
    "WI-QUIC-INT-0002",
    "VER-QUIC-INT-0001",
    "VER-QUIC-INT-0002"
  ],
  "purpose": "Define the interop-runner-facing companion harness contract that hosts Incursa.Quic without moving protocol/runtime ownership out of the main library.",
  "scope": "This slice covers the harness project shape, runner environment parsing, mounted path mapping, endpoint-host shell plumbing, handshake testcase dispatch, unsupported testcase exit behavior, placeholder diagnostics hooks, Docker packaging, and CI participation.",
  "context": "Incursa.Quic remains the owner of reusable transport/runtime behavior. The interop harness still owns process-facing adapter concerns, but it now includes a thin endpoint-host shell that composes the library-owned runtime bridge for real UDP socket plumbing and a handshake-only dispatch path into the managed client/listener bootstrap seam.",
  "open_questions": [
    "Which application protocol and ALPN should the eventual file-transfer endpoint use once Incursa.Quic can drive real end-to-end interop behavior?",
    "When a real TLS provider is selected, what is the practical path for optional SSLKEYLOGFILE support without leaking provider-specific details into the harness?"
  ],
  "supplemental_sections": [
    {
      "heading": "Current Support Posture",
      "content": "The repository now has a thin endpoint-host shell that can bridge one library-owned runtime connection through a real UDP socket boundary, and the managed client/listener host path already owns honest Initial/DCID bootstrap and server Initial-response emission. The harness `handshake` testcase now dispatches into that managed path, while `transfer` and `retry` still return exit code `127` until runner-side enablement exists."
    },
    {
      "heading": "Boundary Split",
      "content": "Library-owned concerns include connection runtime, sender and recovery ownership, TLS-facing transport state, packet protection lifecycle, handshake confirmation, retransmission planning, timer ownership, path-state ownership, packet open/protect helpers, and transport-visible diagnostics hooks. Endpoint-host concerns include the connected UDP socket lifecycle, receive loop, effect-to-socket send plumbing, and ingress/transition/effect observers. Harness-owned concerns include environment parsing, mounted path mapping, role dispatch, process exit codes, Docker packaging, startup scripts, and the thin endpoint-host shell wrapper."
    }
  ],
  "requirements": [
    {
      "id": "REQ-QUIC-INT-0001",
      "title": "Keep the interop endpoint in a companion project",
      "statement": "The interop endpoint MUST be implemented as a companion project that references `Incursa.Quic` rather than by turning the main library project into an application.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0001"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0001"
        ],
        "verified_by": [
          "VER-QUIC-INT-0001"
        ],
        "related": [
          "SPEC-QUIC-CRT"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/Incursa.Quic.InteropHarness.csproj",
          "Incursa.Quic.slnx"
        ]
      },
      "notes": [
        "The harness exists to host the library in the process shape expected by the interop runner.",
        "Reusable transport behavior remains in the library."
      ]
    },
    {
      "id": "REQ-QUIC-INT-0002",
      "title": "Own runner-facing process plumbing in the harness",
      "statement": "The interop harness MUST own parsing of `ROLE`, `TESTCASE`, `REQUESTS`, `QLOGDIR`, and `SSLKEYLOGFILE`, mapping of `/www`, `/downloads`, and `/certs`, role dispatch, and runner-facing process exit-code behavior.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0001"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0001"
        ],
        "verified_by": [
          "VER-QUIC-INT-0001"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/InteropHarnessEnvironment.cs",
          "src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs",
          "src/Incursa.Quic.InteropHarness/Program.cs"
        ]
      },
      "notes": [
        "These concerns exist because of the interop runner contract and would not belong in the library."
      ]
    },
    {
      "id": "REQ-QUIC-INT-0003",
      "title": "Return unsupported testcases honestly",
      "statement": "The interop harness MUST return exit code `127` for any unsupported or not-yet-implemented interop testcase, including `transfer` and `retry` until `Incursa.Quic` can execute those cases end to end for real.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0001"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0001"
        ],
        "verified_by": [
          "VER-QUIC-INT-0001"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs",
          "tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0002.cs"
        ]
      }
    },
    {
      "id": "REQ-QUIC-INT-0004",
      "title": "Keep qlog and keylog hooks honest",
      "statement": "When `QLOGDIR` is provided, the harness MUST wire that setting into a diagnostics placeholder hook without hardwiring the library to qlog JSON, and when `SSLKEYLOGFILE` is not yet practical it MUST remain a clear TODO rather than fake behavior.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0001"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0001"
        ],
        "verified_by": [
          "VER-QUIC-INT-0001"
        ],
        "related": [
          "REQ-QUIC-CRT-0104"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs",
          "src/Incursa.Quic.InteropHarness/README.md",
          "tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0003.cs",
          "tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0004.cs"
        ]
      }
    },
    {
      "id": "REQ-QUIC-INT-0005",
      "title": "Reject invalid runner configuration with failure status",
      "statement": "The interop harness MUST reject invalid `ROLE`, missing `TESTCASE`, or other runner-configuration failures that prevent dispatch with exit code `1` instead of reporting unsupported success.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0001"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0001"
        ],
        "verified_by": [
          "VER-QUIC-INT-0001"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/InteropHarnessEnvironment.cs",
          "src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs",
        "tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0005.cs"
      ]
      }
    },
    {
      "id": "REQ-QUIC-INT-0008",
      "title": "Bridge runtime-owned handshake packets through a real UDP socket shell",
      "statement": "The interop harness MUST provide a thin endpoint-host shell that composes the library-owned connection runtime endpoint through a connected UDP socket, forwards inbound datagrams into the library ingress path, surfaces outbound datagram effects back to the socket layer, and keeps route misses and unsupported cases honest instead of fabricating transport success.",
      "trace": {
        "satisfied_by": [
          "ARC-QUIC-INT-0002"
        ],
        "implemented_by": [
          "WI-QUIC-INT-0002"
        ],
        "verified_by": [
          "VER-QUIC-INT-0002"
        ],
        "related": [
          "SPEC-QUIC-CRT"
        ],
        "x_code_refs": [
          "src/Incursa.Quic.InteropHarness/InteropEndpointHost.cs",
          "src/Incursa.Quic.InteropHarness/InteropHarnessRunner.cs",
          "src/Incursa.Quic.InteropHarness/InteropTlsMaterials.cs",
          "src/Incursa.Quic/QuicConnectionEndpointHost.cs",
          "src/Incursa.Quic/QuicConnectionRuntimeEndpoint.cs",
          "src/Incursa.Quic/QuicConnectionRuntime.cs",
        "tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0008.cs"
      ]
    },
      "notes": [
        "The shell remains adapter-only: it owns socket/process lifecycle and observers, not protocol behavior.",
        "The harness handshake testcase now routes through this seam; transfer and retry still return `127`."
      ]
    }
  ]
}
