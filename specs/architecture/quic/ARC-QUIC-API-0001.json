{
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "artifact_id": "ARC-QUIC-API-0001",
  "artifact_type": "architecture",
  "status": "draft",
  "title": "Incursa.Quic Public API Boundary Architecture",
  "owner": "quic-maintainers",
  "domain": "quic",
  "purpose": "Describe how the connection-, listener-, and first client-entry public facades stay thin while the existing internal protocol runtime, stream-state, and host machinery remain hidden behind a consumer-facing boundary.",
  "related_artifacts": [
    "SPEC-QUIC-API",
    "WI-QUIC-API-0001",
    "VER-QUIC-API-0001"
  ],
  "satisfies": [
    "REQ-QUIC-API-0001",
    "REQ-QUIC-API-0002",
    "REQ-QUIC-API-0003",
    "REQ-QUIC-API-0004",
    "REQ-QUIC-API-0005",
    "REQ-QUIC-API-0006",
    "REQ-QUIC-API-0007",
    "REQ-QUIC-API-0008"
  ],
  "key_components": [
    "docs/design/quic-public-api.md",
    "src/Incursa.Quic/PublicAPI.Shipped.txt",
    "src/Incursa.Quic/PublicAPI.Unshipped.txt",
    "src/Incursa.Quic/QuicConnectionRuntime.cs",
    "src/Incursa.Quic/QuicConnectionStreamState.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeHost.cs",
    "src/Incursa.Quic/QuicListener.cs",
    "src/Incursa.Quic/QuicListenerHost.cs",
    "src/Incursa.Quic/QuicClientConnectionOptions.cs",
    "src/Incursa.Quic/QuicClientConnectionHost.cs",
    "src/Incursa.Quic/QuicConnectionEndpointHost.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeEndpoint.cs",
    "src/Incursa.Quic/QuicListenerOptions.cs",
    "src/Incursa.Quic/QuicServerConnectionOptions.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeShard.cs",
    "src/Incursa.Quic/QuicConnectionSendRuntime.cs",
    "src/Incursa.Quic/QuicTransportTlsBridgeState.cs",
    "src/Incursa.Quic/QuicTlsKeySchedule.cs",
    "src/Incursa.Quic/QuicTlsTranscriptProgress.cs",
    "src/Incursa.Quic/QuicHandshakeFlowCoordinator.cs"
  ],
  "design_summary": "The consumer API is a thin facade over the existing internal protocol engine, and this slice promotes the connection-side surface, the first server-side listener surface, and the first honest client-entry surface. Public connection, stream, listener, and option types own lifetime and configuration capture, while the packet parser, TLS bridge, handshake coordinator, key schedule, transport helpers, and runtime reducers remain internal. The listener shell is implemented by a small internal host that binds a UDP socket, queues accepts, and feeds a narrow server-option callback into provisional connections. The client shell is implemented by a small internal host that creates a client runtime endpoint over a connected UDP socket, bootstraps the existing runtime/TLS seam with the Initial/DCID and inbound Initial handling needed for loopback establishment, and completes the supported positive boundary only on the approved listener path. Full trust-store policy, hostname validation, and the broader data path remain deferred so the facade can land without exposing scheduler or transport state. The stream wrapper is backed by the internal stream-state seam for identity, EOF, and read-side projection; write-heavy behavior waits for the stream-action pipeline.",
  "alternatives_considered": [
    "Expose the existing helper-layer packet, TLS, and runtime types directly. Rejected because it would turn implementation seams into the consumer contract.",
    "Introduce a general middleware or event-pipeline surface. Rejected because the repo already uses a fixed-order transport runtime and the public API should stay consumer-facing.",
    "Mirror every helper type from the current source tree. Rejected because the initial cut should stay narrow and baseline-driven."
  ],
  "edge_cases_and_constraints": [
    "Options must be treated as configuration bags that are consumed by future connect and listen operations, not as live mutable protocol state.",
    "Pending close and stream-read terminal transitions may cancel before commitment; once they commit, the facade must surface the documented terminal outcome instead of retroactively reviving the request.",
    "Listener setup must fail fast on missing mandatory endpoint, ALPN, backlog, or callback inputs.",
    "Pending listener accept may cancel before commitment; once it commits or the listener is disposed, the facade must surface OperationCanceledException or ObjectDisposedException.",
    "The listener connection-options callback remains a narrow server-option selector and must not become a middleware pipeline.",
    "Client connect reuses EndPoint, IPEndPoint, and SslClientAuthenticationOptions directly, but only a narrow supported subset is honored in this slice.",
    "Client connect may remain pending until a peer reaches the supported establishment boundary; this slice does not fabricate connection success.",
    "The public QuicStreamType stays direction-only, even if the internal stream model tracks initiator separately.",
    "All TLS transcript, packet-protection, and runtime reducer details remain internal and are mapped to the public facade rather than exposed directly."
  ],
  "data_and_state_considerations": "The public options objects are copied into internal connection or listener state when the operation begins. The internal listener host owns the socket, accept queue, and shutdown path, while the internal client host owns its connected UDP socket, endpoint host, runtime endpoint, and pending-establishment task. The internal runtime continues to own mutable protocol state, timers, stream accounting, and handshake progress.",
  "risks": [
    "If the public API analyzer files are not trimmed in lockstep, helper-layer types could leak into the shipping surface.",
    "If the listener or client host grows into a general callback pipeline, the consumer contract stops being narrow.",
    "The connection and stream facade types must not grow a middleware-style callback surface by accident.",
    "Any future deviation from the Microsoft ref surface should be documented in the design note before implementation."
  ]
}
