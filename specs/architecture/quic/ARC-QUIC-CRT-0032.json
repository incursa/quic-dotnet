{
  "$schema": "https://github.com/incursa/spec-trace/raw/refs/heads/main/model/model.schema.json",
  "artifact_id": "ARC-QUIC-CRT-0032",
  "artifact_type": "architecture",
  "status": "implemented",
  "title": "QUIC diagnostics sink and qlog adapter boundary architecture",
  "owner": "quic-maintainers",
  "domain": "quic",
  "purpose": "Describe the connection-scoped diagnostics seam, sibling qlog adapter boundary, and narrow host-facing qlog capture helper that keep Incursa.Quic free of qlog serialization and file concerns while still allowing per-connection observational tracing.",
  "related_artifacts": [
    "SPEC-QUIC-CRT",
    "WI-QUIC-CRT-0032",
    "WI-QUIC-CRT-0033",
    "WI-QUIC-CRT-0035",
    "VER-QUIC-CRT-0032",
    "VER-QUIC-CRT-0033",
    "VER-QUIC-CRT-0035"
  ],
  "satisfies": [
    "REQ-QUIC-CRT-0134",
    "REQ-QUIC-CRT-0135",
    "REQ-QUIC-CRT-0136",
    "REQ-QUIC-CRT-0138"
  ],
  "key_components": [
    "src/Incursa.Quic/QuicDiagnostics.cs",
    "src/Incursa.Quic/QuicConnectionRuntime.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeEventModels.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeEndpoint.cs",
    "src/Incursa.Quic/QuicConnectionRuntimeHost.cs",
    "src/Incursa.Quic/QuicClientConnectionHost.cs",
    "src/Incursa.Quic/QuicListenerHost.cs",
    "src/Incursa.Quic/QuicConnection.cs",
    "src/Incursa.Quic.Qlog/QuicQlogCapture.cs",
    "src/Incursa.Quic.Qlog/QuicQlogDiagnosticsSink.cs",
    "src/Incursa.Quic.Qlog/Incursa.Quic.Qlog.csproj"
  ],
  "risks": [
    "If the sink contract remains string-only, the adapter will not have enough stable transport facts to map cleanly to qlog builders.",
    "If per-connection enablement is resolved lazily inside the reducer, packet, stream, timer, and path paths will pay repeated policy checks.",
    "If the adapter owns serializer or file logic, qlog output concerns will leak back into the transport core through package dependencies."
  ],
  "design_summary": "The transport core resolves one diagnostics sink per connection during connection setup and stores only the resolved sink inside the runtime. The runtime emits structured transport facts through that sink, but it never references qlog builders, JSON writers, or file paths. A sibling Incursa.Quic.Qlog adapter package depends on Incursa.Quic, Incursa.Qlog, and Incursa.Qlog.Quic, registers the qlog schema once per trace, and converts the transport facts into qlog events and trace append operations. A narrow host-facing capture helper above that adapter can aggregate one or many traces in memory and hand the resulting qlog file to the existing JSON serializer without moving file rotation, serializer choice, or QLOGDIR ownership into the transport core. When a caller serializes captured output, the helper snapshots the mutable file and trace state under its capture gate and performs the JSON write after releasing that gate so live diagnostic append paths are not pinned behind serializer or filesystem latency.",
  "alternatives_considered": [
    "Put qlog adapters directly in Incursa.Quic. Rejected because it would add qlog and serializer dependencies to the core package and contaminate the hot path.",
    "Use one global diagnostics registry. Rejected because it would require repeated configuration checks and make per-connection targeting coarse.",
    "Emit qlog files from the runtime. Rejected because file and serializer concerns belong outside the transport core."
  ],
  "edge_cases_and_constraints": [
    "Diagnostics disabled by default must not allocate qlog objects or format strings.",
    "Per-connection sink resolution must happen before the runtime starts processing events.",
    "The adapter may omit qlog fields when transport facts are unavailable; it must not invent data.",
    "qlog schema registration must happen once per trace, not once per event.",
    "The adapter must not influence scheduling, routing, or recovery behavior.",
    "A host-facing capture helper may own the in-memory `QlogFile` envelope and collected `QlogTrace` instances, but it must keep file policy above the transport core.",
    "Serialization must operate on a stable snapshot rather than holding the live append lock across the full JSON write."
  ],
  "data_and_state_considerations": "The client and listener hosts are the natural sink-resolution points because they already own connection setup and endpoint metadata. The runtime should store only the resolved sink or null object, while the adapter package owns the qlog trace and per-trace event append order. The consumer or host chooses whether to serialize one trace or many traces after the adapter has captured the events, and the host-facing capture helper may aggregate that in-memory file before handing it to the serializer. That helper must clone the mutable qlog envelope, traces, and events while holding the capture gate only long enough to obtain a stable snapshot, then release the gate before the serializer or filesystem work begins.",
  "supplemental_sections": [
    {
      "heading": "Scope",
      "content": "This slice is observational only. It does not add qlog file emission, serializer choice, or harness QLOGDIR plumbing inside Incursa.Quic."
    },
    {
      "heading": "Dependency Direction",
      "content": "- `Incursa.Quic` depends on no qlog packages.\n- `Incursa.Quic.Qlog` depends on `Incursa.Quic`, `Incursa.Qlog`, and `Incursa.Qlog.Quic`.\n- File and serializer consumers depend on `Incursa.Qlog.Serialization.Json` outside the transport core.\n- The adapter owns `QlogTrace` and `QlogEvent` append flow, not file output."
    },
    {
      "heading": "Mapping Sketch",
      "content": "- Connection lifecycle and phase changes map to `quic:connection_started`, `quic:connection_state_updated`, and `quic:connection_closed`.\n- Packet ingress, egress, and parse failures map to `quic:packet_received`, `quic:packet_sent`, and `quic:packet_dropped`.\n- Transport-parameter commits map to `quic:parameters_set` or `quic:parameters_restored`.\n- Path classification and validation map to `quic:migration_state_updated`.\n- Connection-ID lifecycle events map to `quic:connection_id_updated`.\n- Key lifecycle events map to `quic:key_updated` and `quic:key_discarded`.\n- Stream state and movement map to `quic:stream_state_updated`, `quic:stream_data_moved`, and `quic:datagram_data_moved`.\n- Recovery and congestion facts map to `quic:recovery_parameters_set`, `quic:recovery_metrics_updated`, `quic:congestion_state_updated`, and `quic:packet_lost`."
    },
    {
      "heading": "Related Code And Tests",
      "content": "- [`src/Incursa.Quic/QuicDiagnostics.cs`](../../../src/Incursa.Quic/QuicDiagnostics.cs)\n- [`src/Incursa.Quic/QuicConnectionRuntime.cs`](../../../src/Incursa.Quic/QuicConnectionRuntime.cs)\n- [`src/Incursa.Quic/QuicConnectionRuntimeEventModels.cs`](../../../src/Incursa.Quic/QuicConnectionRuntimeEventModels.cs)\n- [`src/Incursa.Quic/QuicConnectionRuntimeEndpoint.cs`](../../../src/Incursa.Quic/QuicConnectionRuntimeEndpoint.cs)\n- [`src/Incursa.Quic/QuicConnectionRuntimeHost.cs`](../../../src/Incursa.Quic/QuicConnectionRuntimeHost.cs)\n- [`src/Incursa.Quic/QuicClientConnectionHost.cs`](../../../src/Incursa.Quic/QuicClientConnectionHost.cs)\n- [`src/Incursa.Quic/QuicListenerHost.cs`](../../../src/Incursa.Quic/QuicListenerHost.cs)\n- [`src/Incursa.Quic/QuicConnection.cs`](../../../src/Incursa.Quic/QuicConnection.cs)\n- [`tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0104.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0104.cs)\n- [`tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0004.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/INT/REQ-QUIC-INT-0004.cs)\n- [`tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0134.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0134.cs)\n- [`tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0135.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0135.cs)\n- [`tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0136.cs`](../../../tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0136.cs)"
    }
  ]
}
