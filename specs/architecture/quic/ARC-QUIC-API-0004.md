<!-- Generated from sibling .json by scripts/spec-trace/Render-SpecTraceMarkdownFromJson.ps1. Do not edit this file directly. -->

# ARC-QUIC-API-0004 - Incursa.Quic Standard Client TLS Validation Architecture

## Purpose

Describe the narrowest honest standard public client-validation shape on the existing `SslClientAuthenticationOptions` carrier without widening the public API or the exact-pinning floor.

## Requirements Satisfied

- REQ-QUIC-API-0013

## Design Summary

The mainstream client validation path lives on the existing `SslClientAuthenticationOptions` carrier. The validator captures `TargetHost`, `CertificateChainPolicy`, `CertificateRevocationCheckMode`, and callback overrides, the client host carries the captured settings into the runtime, and the TLS bridge driver uses BCL X509 chain evaluation plus hostname matching to produce the final accept or fail-closed decision. The exact-pinning `QuicPeerCertificatePolicy` path remains separate and mutually exclusive with the standard path.

## Key Components

- docs/design/quic-public-api.md
- docs/design/quic-public-api-gap-matrix.md
- docs/design/quic-interop-prep-plan.md
- src/Incursa.Quic/QuicPeerCertificatePolicy.cs
- src/Incursa.Quic/QuicClientConnectionOptions.cs
- src/Incursa.Quic/QuicClientConnectionOptionsValidator.cs
- tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0005.cs
- tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0012.cs
- tests/Incursa.Quic.Tests/RequirementHomes/QUIC/REQ-QUIC-API-0013.cs
- tests/Incursa.Quic.Tests/RequirementHomes/CRT/REQ-QUIC-CRT-0123.cs

## Data and State Considerations

The current public carrier already snapshots exact-match inputs before the runtime path begins. The standard validation path now preserves and clones the BCL auth options, including `TargetHost` and `CertificateChainPolicy`, before the runtime bridge evaluates the peer certificate. The exact-pinning path continues to use the existing snapshot seam and remains fail closed on mismatch.

## Edge Cases and Constraints

- The mainstream validation path and the exact-pinning floor are mutually exclusive so the validator can reject ambiguous mixed configuration deterministically.
- Hostname matching is applied only when `TargetHost` is supplied; the bridge normalizes the host name and uses BCL hostname matching on the presented leaf certificate.
- When no explicit chain policy is supplied, revocation checking follows `CertificateRevocationCheckMode`; when a chain policy is supplied, the chain policy clone governs trust evaluation.
- Unsupported broader client-auth knobs remain out of scope and still reject deterministically.

## Alternatives Considered

- Add a separate top-level public options object for hostname and trust-anchor validation: rejected because the existing `SslClientAuthenticationOptions` carrier already maps cleanly to the standard BCL-shaped client validation path.
- Reuse `QuicPeerCertificatePolicy` for the mainstream path: rejected because the exact-pinning floor must remain distinct from the standard BCL validation inputs.
- Model the standard path with a new QUIC-specific carrier: rejected because it would duplicate an existing BCL-shaped surface without adding clarity.

## Risks

- If the validator still rejects the standard inputs, the mainstream BCL-shaped path cannot be exercised honestly.
- If mixed-mode configuration is allowed, consumers will not know whether exact pinning or standard validation applies.
- If the bridge computes policy errors without building a real chain and matching the host name, the standard path will overclaim support.
