{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://incursa.dev/schemas/adaptive-runtime-actor-service-observation-v4.schema.json",
  "title": "Adaptive Runtime Actor Service Observation v4",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "serviceSequence",
    "shardIndex",
    "wakeSequence",
    "wakePosition",
    "wakeCompletion",
    "wakeSource",
    "workKind",
    "disposition",
    "queueDelayMicros",
    "serviceTimeMicros",
    "pendingWorkItemsAfterDequeue",
    "effectCount",
    "applicationSendFollowOnCount",
    "flowControlFollowOnCount",
    "streamCapacityFollowOnCount",
    "phaseAfter",
    "disposalStarted",
    "validity",
    "interServiceGapMicros",
    "deadlineLatenessMicros",
    "serviceContenderCountAtStart",
    "acceptedConnectionWorkItemsAfterCurrent",
    "observationContractVersion",
    "provenanceVersion"
  ],
  "properties": {
    "serviceSequence": { "$ref": "#/$defs/positiveInteger" },
    "shardIndex": { "$ref": "#/$defs/unsignedInteger" },
    "wakeSequence": { "$ref": "#/$defs/positiveInteger" },
    "wakePosition": { "$ref": "#/$defs/positiveInteger" },
    "wakeCompletion": {
      "enum": ["ConsumerStart", "Synchronous", "Asynchronous"]
    },
    "wakeSource": {
      "enum": ["ConsumerStart", "Inbox", "Deadline"]
    },
    "workKind": {
      "enum": [
        "ConnectionEvent",
        "Timer",
        "PacketReceived",
        "StreamCapacityRelease",
        "FlowControlCreditUpdate",
        "StreamOpen",
        "StreamWrite"
      ]
    },
    "disposition": {
      "enum": [
        "Completed",
        "SkippedDisposed",
        "SkippedIndependentConsumer",
        "Faulted"
      ]
    },
    "queueDelayMicros": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "serviceTimeMicros": { "$ref": "#/$defs/unsignedInteger" },
    "pendingWorkItemsAfterDequeue": {
      "$ref": "#/$defs/unsignedInteger"
    },
    "effectCount": { "$ref": "#/$defs/unsignedInteger" },
    "applicationSendFollowOnCount": {
      "$ref": "#/$defs/unsignedInteger"
    },
    "flowControlFollowOnCount": {
      "$ref": "#/$defs/unsignedInteger"
    },
    "streamCapacityFollowOnCount": {
      "$ref": "#/$defs/unsignedInteger"
    },
    "phaseAfter": {
      "enum": ["Establishing", "Active", "Closing", "Draining", "Discarded"]
    },
    "disposalStarted": { "type": "boolean" },
    "validity": { "type": ["string", "integer"] },
    "interServiceGapMicros": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "deadlineLatenessMicros": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "serviceContenderCountAtStart": {
      "type": ["integer", "null"],
      "minimum": 1
    },
    "acceptedConnectionWorkItemsAfterCurrent": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "observationContractVersion": {
      "const": "quic-actor-service-observation-v4"
    },
    "provenanceVersion": {
      "const": "quic-actor-service-provenance-v4"
    }
  },
  "$defs": {
    "unsignedInteger": {
      "type": "integer",
      "minimum": 0
    },
    "positiveInteger": {
      "type": "integer",
      "minimum": 1
    }
  }
}
